{% extends 'base.html.twig' %}

{% block body %}
<h2 class="text-center mt-4">{{ title }} un membre</h2>
<div class="container mt-4">
	<div class="row">
		<div class="col-12">
			<div class="col-12">
				{{ form_start(form) }}
				{{ form_row(form.firstname) }}
				{{ form_row(form.lastname) }}
				{{ form_row(form.email) }}
				{{ form_row(form.address) }}
				<div class="">
					<button type="submit" class="btn btn-primary">
						Enregistrer
					</button>
				</div>
				{{ form_end(form) }}
			</div>
		</div>
	</div>
</div>

{% endblock %}
